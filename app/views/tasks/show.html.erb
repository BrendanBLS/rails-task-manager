<h1><%= @task.title %></h1>
<h2><%= @task.details %></h2>
<h3><%= @task.completed ? "completed" : "Not completed" %></h3>
<%= link_to "edit task", edit_task_path %>
<%= link_to "Delete task", task_path(@task),
data: {turbo_method: :delete, turbo_confirm: "Sure bro?"} %>
<%= link_to "back", tasks_path %>
